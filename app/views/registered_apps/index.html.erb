<div class="container">
    </ul>
  </div>
  <h2>All your apps:</h2>
    <div class="table-responsive">
      <table class="table table-hover ">
        <%= render @user.registered_apps %>
      </table>
    </div>
  <div class='new-registered-apps'>
      <%= render 'registered_apps/form', registered_app: RegisteredApp.new, user: current_user %>
  </div>
</div>
